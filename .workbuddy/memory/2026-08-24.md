# 2026-08-24 工作日志

## Media 项目结构分析与登录排查
用户反映 http://47.121.26.8:8080/login 无账号密码无法登录，要求分析项目结构并定位账号密码文件与数据存储位置。

完成事项：
- 通读 README、package.json、docker-compose.yml、drizzle.config.ts、.env.example、db/schema.ts、db/index.ts、app/login/page.tsx、app/api/auth/(login|bootstrap)/route.ts、app/page.tsx
- 结论：项目无预置账号密码，采用"首位访客自助创建管理员"机制；密码用 Argon2 哈希存于 PostgreSQL `users.password_hash`，明文不存在于任何文件
- 数据存储：PostgreSQL 17 跑在 Docker `db` 容器内，数据卷 `media_creator_os_postgres_data`；数据库密码在服务器 `/opt/media-platform/shared/.env`（POSTGRES_PASSWORD）
- 向用户给出三种落地方案：先看登录页是创建还是登录表单 → 若已锁死则 SSH 进容器查/重置 users 表

## 服务器IP变更与写死IP排查
用户担心阿里云公网IP常变、每次需在GitHub更新授权，要求检查代码写死IP并更新。

完成事项：
- curl 测试 47.121.26.8:8080 → HTTP 200，当前IP仍有效、服务正常
- 全库搜 IP 正则：唯一"写死真实IP"处为 `next.config.ts:6` 的 `allowedDevOrigins: ["192.168.0.102"]`（局域网开发IP，仅影响 next dev 的 HMR，与公网/生产无关）
- 其余 IP 均为合规用法：127.0.0.1（本机回环/健康检查）、0.0.0.0（端口绑定通配）；公网IP走环境变量 `NEXT_PUBLIC_APP_URL` 与 GitHub Secret `DEPLOY_HOST`
- 代码层面**无写死公网服务器IP**；IP变更需更新两处配置：①GitHub Secrets 的 DEPLOY_HOST（+实例重建则更新 DEPLOY_HOST_KEY）②服务器 /opt/media-platform/shared/.env 的 NEXT_PUBLIC_APP_URL
- `docs/操作手册.md` 第二节已含完整 SSH 主机身份变更处置流程（含 gh secret set 示例，仓库名 qiubo-master/Media）
- 待用户确认后改 next.config.ts：把 allowedDevOrigins 改为环境变量驱动（process.env.ALLOWED_DEV_ORIGINS 按逗号分割）

## CICD 中台发布 Media 项目流程
用户问在 CICD 中台点"发布"要改/确认哪些，要求一步步说明。

梳理结论（基于 ci.yml + deploy.yml + ops/deploy-media.sh + 操作手册第一节）：
- 发布前置三块：①GitHub Secrets（DEPLOY_HOST/PORT/USER/SSH_KEY/HOST_KEY，IP变改DEPLOY_HOST）②服务器 /opt/media-platform/shared/.env（POSTGRES_PASSWORD必设、NEXT_PUBLIC_APP_URL=当前公网IP）③main 分支 Media CI 全绿（lint/test/build）
- 中台发布时 deploy.yml 的 inputs：action=deploy、resource_profile、app_cpu(1.0/2.0/4.0)、app_memory(1g/2g/4g)、database_memory(512m/1g/2g)、host_port(默认8080)、bind_address(0.0.0.0直连公网 / 127.0.0.1走宿主Nginx)
- 发布后检查：curl http://IP:8080/api/health=200、登录测试、docker compose ps 容器健康
- 迁移由 migrate 服务（compose up 时一次性）自动执行，数据卷不随版本替换删除
- 回滚用 deploy.yml 选 action=rollback，只切版本不回滚DB

## 统一默认分支 main → master
用户指出 GitHub 仓库默认分支是 master，要求统一代码里写 main 的地方。
- 全库搜 main 分支引用：仅两处 → `.github/workflows/ci.yml:6`（`branches: [main]`→`[master]`）、`docs/操作手册.md:5`（"GitHub `main` 分支"→"`master` 分支"）
- deploy.yml 是 workflow_dispatch 手动触发，无分支限制，不动；README/scripts/tests/ARCHITECTURE 均无 main 引用
- 已改完并 git diff 展示，等用户确认后自行提交部署
